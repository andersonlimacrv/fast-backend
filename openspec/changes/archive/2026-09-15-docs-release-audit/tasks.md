## 1. Verificação (só leitura)

- [x] 1.1 CHANGELOG × `git tag` × `pyproject.toml` version × `APP_VERSION` default
- [x] 1.2 ROADMAP fases × changes arquivadas × specs em `openspec/specs/` (contagem)
- [x] 1.3 ADRs 0001–0010: status, links, sem lacuna de numeração
- [x] 1.4 READMEs (raiz + client) × comportamento atual (`make dev`, rotas, flags)
- [x] 1.5 `.env.example` × `Settings` (todas as chaves?) + links internos quebrados

## 2. Correções + gates

- [x] 2.1 Sync de conteúdo + decisão CHANGELOG registrada
- [x] 2.2 `openspec verify` 3 dimensões antes de `archive`
